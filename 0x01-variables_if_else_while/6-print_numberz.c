#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Sucess)
*/

int main(void)
{
	int counter;

	for (counter = 48; counter < 58; counter++)
		putchar(counter);

	putchar(counter);



	return (0);
}
